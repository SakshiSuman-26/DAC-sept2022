											NOTES OF CONSTRUCTOR CALL PARENT AND CHILD

we can call explicitly 1 arg parent cons by using super(a)
super() 0 arg parent cons will be called 
not necessary bcoz it implicitly call 0 arg parent cons

the super statement will always be the first statement inside "child cons"



non static method se cons call nahi kar sakte kyuki 
it will throw error
cons call hota hai object creation ke waqt (object creation ke bad call nhi hota cons)
 but 
non static method call hota hai object creation ke baad



super() and this() ek sath use nahi kar sakte kyuki dono ka first statement
hone jaruri hai
dono ek sath karne se rule violet hoga.

super() se sirf cons call ho sakta hai.